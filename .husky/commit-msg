# Load NVM in non-interactive shell (skip if using Volta or another manager)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Use the Node version from .nvmrc (only if nvm is available)
if command -v nvm &> /dev/null; then
  nvm use
fi

npx commitlint --edit "$1"

